set default=0
set gfxpayload=keep
set timeout=10
insmod all_video

menuentry 'Boot LiveCD (kernel: gentoo)' --class gnu-linux --class os {
        linux gentoo dokeymap looptype=squashfs loop=/image.squashfs cdroot
        initrd combined.igz
}
